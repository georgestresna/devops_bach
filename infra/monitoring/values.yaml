prometheus:
  prometheusSpec:
    retention: 2d
    resources:
      requests:
        memory: 256Mi
      limits:
        memory: 400Mi
    # dezactivezi componente grele neesentiale acum
grafana:
  resources:
    requests:
      memory: 128Mi
      limits: 256Mi
alertmanager:
  enabled: false   # il activezi cand ai resurse